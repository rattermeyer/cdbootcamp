- hosts: dockerhosts
  pre_tasks:
    - name: "install aufs"
      apt:
        name: aufs-tools
  roles:
    - role: rattermeyer.docker
      docker_group_members: vagrant
      docker_opts: >
        -H unix://
        -H tcp://0.0.0.0:2375
        --insecure-registry 192.168.56.22:5000
